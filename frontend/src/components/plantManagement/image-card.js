import React from 'react';
import { Card } from 'antd';

const cardStyle = {
  width: '100%',
  height: '100%',
};

const ImageCard = () => {
  return <Card hoverable style={cardStyle} />;
};

export default ImageCard;
